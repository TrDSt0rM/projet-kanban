package com.example.servertomcat.user.entities;

/**
 * Enumération représentant les rôles d'un utilisateur dans le système.
 * ADMIN : Un administrateur avec tous les droits.
 * USER : Un utilisateur régulier avec des droits limités.
 */
public enum UserRole {
    ADMIN,
    USER,
}
